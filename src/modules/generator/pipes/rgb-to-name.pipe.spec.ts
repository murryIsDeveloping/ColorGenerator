import { RgbToNamePipe } from './rgb-to-name.pipe';

describe('RgbToNamePipe', () => {
  it('create an instance', () => {
    const pipe = new RgbToNamePipe();
    expect(pipe).toBeTruthy();
  });
});
