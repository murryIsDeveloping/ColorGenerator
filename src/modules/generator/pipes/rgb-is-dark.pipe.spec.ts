import { RgbIsDarkPipe } from './rgb-is-dark.pipe';

describe('RgbIsDarkPipe', () => {
  it('create an instance', () => {
    const pipe = new RgbIsDarkPipe();
    expect(pipe).toBeTruthy();
  });
});
