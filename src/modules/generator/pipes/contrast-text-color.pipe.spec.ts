import { ContrastTextColorPipe } from './contrast-text-color.pipe';

describe('ContrastTextColorPipe', () => {
  it('create an instance', () => {
    const pipe = new ContrastTextColorPipe();
    expect(pipe).toBeTruthy();
  });
});
